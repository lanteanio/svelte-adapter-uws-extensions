/** Wire-protocol event names this module emits. */
export const EVENTS = Object.freeze({
	CATALOG: 'catalog',
	JOIN: 'join',
	UPDATE: 'update',
	REMOVE: 'remove',
	BULK: 'bulk',
	TIME: 'time',
	YOU: 'you'
});
